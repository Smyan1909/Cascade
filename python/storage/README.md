# storage
Shared Firestore wrappers and checkpointing helpers for all agents. Implement per persistence rules in `docs/00-overview.md`.

## Modules
- `firestore_client.py`: scoped CRUD helpers
- `vector_store.py`: lightweight in-memory embeddings for manual indexing

